// Centralized place for provider API tokens / base URLs.
// Keep minimal and use environment variables when possible.

export default {
  // Example placeholders for future providers:
  // DDB_API_TOKEN: process.env.DDB_API_TOKEN || '',
  // OPEN5E_BASE_URL: process.env.OPEN5E_BASE_URL || 'https://api.open5e.com',
};
