{
  "name": "dnd-beyond-mcp-server",
  "version": "0.1.0",
  "type": "module",
  "description": "MCP server for TTRPG providers (D&D Beyond JSON import + extensible providers)",
  "scripts": {
    "start": "node index.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.1",
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
